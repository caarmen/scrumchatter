<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.rmen.android.scrumchatter"
    android:versionCode="100"
    android:versionName="@string/app_version" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ca.rmen.android.scrumchatter.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ca.rmen.android.scrumchatter.MeetingActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ca.rmen.android.scrumchatter.MainActivity" />
        </activity>

        <activity
            android:name="ca.rmen.android.scrumchatter.AboutActivity"
            android:label="@string/about"
            android:parentActivityName="ca.rmen.android.scrumchatter.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ca.rmen.android.scrumchatter.MainActivity" />
        </activity>

        <provider
            android:name="ca.rmen.android.scrumchatter.provider.ScrumChatterProvider"
            android:authorities="ca.rmen.android.scrumchatter.provider"
            android:exported="false" >
        </provider>

    </application>

</manifest>